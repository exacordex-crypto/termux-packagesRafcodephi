# RAFCODEPHI Contract

`RAFCODEPHI_CORE_PACKAGES.env` declares the core package set required by the app repository for a real runtime payload.

The app should pin this repository to a commit and validate that file before release builds.
